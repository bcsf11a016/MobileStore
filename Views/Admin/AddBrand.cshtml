@*@model XtremeMobiles.Models.Category
@{
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<div class="panel panel-default">
    <div class="panel-heading">
        <strong>Add Brand</strong>
    </div>
    <div class="panel-body">

        @using (Html.BeginForm("AddBrandToDb", "Admin", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.AntiForgeryToken()

            @Html.ValidationSummary(true)
            <div class="form-group">
                @Html.LabelFor(m => m.Name, new { @class = "col-md-2 col-xs-2 col-sm-2 control-label" })
                <div class="col-md-10 col-xs-10 col-sm-10">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                </div>

                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10 col-xs-offset-2 col-xs-10 col-sm-offset-2 col-sm-10">
                        <input type="submit" class="btn btn-default" value="Submit" />
                    </div>
                </div>
            </div>
        }

    </div>
</div>*@
